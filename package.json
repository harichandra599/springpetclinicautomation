{
  "name": "springpet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "open": "cypress open", 
    "cy:verify": "cypress verify", 
    "cy:info": "cypress info", 
    "cy:test": "cypress open --env fileConfig=test", 
    "combine:report": "mochawesome-merge ./cypress/results/*.json -o index.json", 
    "generate:report": "marge index.json --reportDir public --assetsDir public/assets --reportPageTitle index.html", 
    "cy:dev:regression": "cypress run -e TAGS=\"@regression\" --browser chrome --headed"
    
  },
  "author": "Hari",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "^12.14.0",
    "cypress-xpath": "^2.0.1",
    "esbuild": "^0.18.2"
  }
}
